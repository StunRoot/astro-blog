---
import BaseLayout from '../layouts/BaseLayout.astro';
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog')).sort(
  (a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf()
);
---
<BaseLayout title="Home">
  <h1>Polt.StunRoot</h1>
  <p>I write about the projects I build and what I learn along the way — part portfolio, part documentation for future me.</p>

  <h2 class="groupbox">Recent Posts</h2>
  <div class="listview">
    <div class="listview-header">
      <span>Name</span>
      <span>Date Modified</span>
    </div>
    {posts.map((post) => (
      <a class="listview-row" href={`/blog/${post.slug}/`}>
        <span class="fname">
          <span class="icon"></span>
          {post.data.title}
        </span>
        <span class="fdate">{post.data.pubDate.toISOString().slice(0, 10)}</span>
        <span class="post-desc">{post.data.description}</span>
      </a>
    ))}
  </div>
</BaseLayout>